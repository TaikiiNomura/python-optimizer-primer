"""
- y=x^2 の最小値を勾配降下法で探すプログラム
- ステップが進むごとに変数が0に近づくことを確認する
"""

lr = 0.1
x = 3 # 変数の初期値。この値を更新する
for i in range(1, 11):
    y = x ** 2 #
    grad = 2 * x # 勾配計算
    x = x - lr * grad # パラメータを更新
    print(f"現在のステップ{i} | 現在の変数 x={x:.3f} | 現在の関数値:y={x**2}") # 今回の変数を出力
